# Crypto Tool Deployment Configuration
# This file is created by the Jenkins pipeline for deployment to /opt/crypto-thing/
CRYPTO_CONFIG_FILE=/etc/crypto-thing/crypto.ini

# Database configuration (will be overridden by config file)
# DATABASE_URL=postgres://username:password@localhost:5432/crypto_thing?sslmode=disable

# Coinbase API configuration (will be overridden by config file)
# COINBASE_API_KEY=your_api_key
# COINBASE_API_SECRET=your_api_secret
# COINBASE_PASSPHRASE=your_passphrase

# Rate limiting configuration (will be overridden by config file)
# COINBASE_RPM=10
# COINBASE_MAX_RETRIES=3
# COINBASE_BACKOFF_MS=500
